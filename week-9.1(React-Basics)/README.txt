reactjs:
-->we usually have to define all your components once and then all you have to do is update the state of your app. React takes care of re-rendering your app.

--> if you want to work with reactjs. we need to worry about two things:
1)state (libraries - redux, recoil)
2)Components 

-->for a static website all you need is components .whereas for dynamic website we need both state and components

-->a state change triggers a re-render .A re-render represents the actual DOM being manipulated whent the state changes.

terms: state/ components/ re-rendering