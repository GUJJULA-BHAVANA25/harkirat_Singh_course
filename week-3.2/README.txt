Complex DOM manipulation:
-->creating a DOM element which has another DOM element inside



While doing todo list:
-->until now we did :
   writing a text span and button elements and appending to the div and then appending to body
   now we are dealing with raw html as a string
-->but the second approach is creating span tag and button tag .appending 
   them to div and appending div to the body
   here we are dealing with div (DOM approach)



   state:the variable part of an app.for example, linkedin posts, when we open linkedin all the new posts come to our page.all the posts blueprint is same but what is the variable is that name of the person posted ,at what time ,how many likes .these are the things thatt are different from perosn to person.

   components: how to render state on screen

   rendering:taking the state and rendering it on the DOM based on the components